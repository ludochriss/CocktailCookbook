runtime: aspnetcore

env: flex
env_variables:
 DB_USER: "app-engine"
 DB_NAME: "cookbook-db-v2"
 DB_PASS: "cookbook"
 DB_HOST: "34.116.96.157"

beta_settings:
 cloud_sql_instances: "eloquent-surge-324405:australia-southeast1:cookbook-db=tcp:4001"

# This sample incurs costs to run on the App Engine flexible environment. 

# The settings below are to reduce costs during testing and are not appropriate

# for production use. For more information, see:

# https://cloud.google.com/appengine/docs/flexible/dotnet/configuring-your-app-with-app



#resources:
#
#  cpu: 2
#
#  memory_gb: 2.4
#
#  disk_size_gb: 10